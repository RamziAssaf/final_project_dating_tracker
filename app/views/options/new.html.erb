<!-- Validation failure messages -->
<% if @option.errors.any? %>
  <% @option.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h1>Add Outing Idea</h1>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_option" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for title -->
      <div class="form-group">
        <label for="title" class="control-label">
          Title
        </label>

        <textarea id="title" name="title" placeholder="Title" class="form-control" rows="3"><%= @option.title %></textarea>
      </div>

      <button class="btn btn-success">
        Add
      </button>
      or
      <a href="/options">Cancel</a>
    </form>
  </div>
</div>
